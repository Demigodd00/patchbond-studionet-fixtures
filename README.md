# PatchBond StudioNet fixtures

Synthetic fixtures for real GenLayer StudioNet patch-review tests. No production assets or real-value funds.

The frozen workflow runs smoke tests, not complete acceptance tests. A green CI run is necessary but deliberately insufficient: GenLayer independently reviews the full changed source against the frozen requirements. Each patch branch is a single child of the registered base. Test code is not a production library.
